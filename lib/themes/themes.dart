import 'package:flutter/material.dart';

class ThemeApp{
  static Color darkColor = Colors.grey[800];
  static Color primaryColor = Color.fromRGBO(5, 105, 107, 1);
  static Color disableColor = Colors.grey[300];
}